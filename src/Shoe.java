import java.sql.Connection;

public class Shoe extends Product
{
    @Override
    public void UpdateProduct(String queryObject, Connection conn)
    { //this may be wrong at first, for now its just a general skeleton

    }
}
